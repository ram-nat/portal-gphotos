<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark, no action bar. Portal requires a dark top region so the white
         system overlay pills (back/home/wifi) stay visible. -->
    <style name="Theme.PortalGphotos" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
